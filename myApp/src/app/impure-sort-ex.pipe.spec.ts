import { ImpureSortExPipe } from './impure-sort-ex.pipe';

describe('ImpureSortExPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureSortExPipe();
    expect(pipe).toBeTruthy();
  });
});
