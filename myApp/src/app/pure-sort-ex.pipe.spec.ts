import { PureSortExPipe } from './pure-sort-ex.pipe';

describe('PureSortExPipe', () => {
  it('create an instance', () => {
    const pipe = new PureSortExPipe();
    expect(pipe).toBeTruthy();
  });
});
